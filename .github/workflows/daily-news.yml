name: 每日新闻监测

on:
  schedule:
    # 每天 UTC 0点 = 北京时间 8点
    - cron: '0 0 * * *'
  # 允许手动点击运行（测试用）
  workflow_dispatch:

jobs:
  run-radar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

            - name: 🔍 深度调试目录结构
        run: |
          echo "=== 1. 当前目录 ==="
          pwd
          echo "=== 2. 根目录下所有文件 ==="
          ls -la
          echo "=== 3. src 文件夹内部详情 ==="
          ls -la src/
          echo "=== 4. 检查 __init__.py 是否在里面 ==="
          cat src/__init__.py || echo "警告：src里没有__init__.py！"
          echo "=== 5. 检查是否有 src.py 捣乱 ==="
          ls -la src.py 2>/dev/null && echo "危险：根目录有 src.py 文件！" || echo "安全，没有 src.py 干扰"
          
      - name: 创建 .env 文件（从 Secrets 注入配置）
        run: |
          echo "DEEPSEEK_API_KEY=${{ secrets.DEEPSEEK_API_KEY }}" >> .env
          echo "DEEPSEEK_MODEL=${{ secrets.DEEPSEEK_MODEL || 'deepseek-chat' }}" >> .env
          echo "DEEPSEEK_BASE_URL=${{ secrets.DEEPSEEK_BASE_URL || 'https://api.deepseek.com' }}" >> .env
          echo "SMTP_HOST=${{ secrets.SMTP_HOST || 'smtp.qq.com' }}" >> .env
          echo "SMTP_PORT=${{ secrets.SMTP_PORT || '465' }}" >> .env
          echo "SMTP_USER=${{ secrets.SMTP_USER }}" >> .env
          echo "SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }}" >> .env
          echo "MAIL_TO=${{ secrets.MAIL_TO }}" >> .env
          echo "MAIL_FROM=${{ secrets.MAIL_FROM }}" >> .env
          echo "MIN_SCORE_TO_EMAIL=${{ secrets.MIN_SCORE_TO_EMAIL || '75' }}" >> .env
          echo "MAX_EMAIL_EVENTS=${{ secrets.MAX_EMAIL_EVENTS || '10' }}" >> .env
          echo "DB_PATH=market_events.db" >> .env
      
      - name: 运行主程序
        run: python -m src.main
